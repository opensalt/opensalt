#!/usr/bin/env bash
#<?php /*

if [ ! -f /.dockerenv ]; then
  exec $(dirname $0)/phpcli ./vendor/bin/phpspec "$@"
fi

exec php $(dirname $0)/../vendor/bin/phpspec "$@"

*/
require 'vendor/bin/phpspec';
