{
    "itemId":{{ lsItem.id|json_encode|raw }}
    {% if lsItem.children is not empty %}
        ,"children": [
            {% for childWithId in lsItem.children %}
                {% if not loop.first %}, {% endif %}
                {% set child = items[childWithId.id] %}
                {% include 'CftfBundle:DocTree:render_update_item_child.json.twig' with { 'lsItem':child, 'items':items } %}
            {% endfor %}
        ]
    {% endif %}
}
