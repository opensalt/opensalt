<div>
	<p>Export Competency Framework “<b>{{ lsDoc.title }}</b>” as:</p>
	<div class="container-fluid" style="margin:15px 0"><div class="row">
		<div class="col-xs-3"><a href="{{ path('cfpackage_export2', {'id':lsDoc.id, '_format':'json'}) }}" target="_blank" role="button" tabindex="1" class="btn btn-primary btn-block" style="white-space:normal; height:3.8em"  data-toggle="popover" data-trigger="hover" data-placement="bottom" data-html="true" data-content="Exports a JSON file using the IMS-standard format. This is the best format to use for archiving Frameworks.<hr>The JSON file will open in a separate window; use your browsers “Save As” functionality to save the JSON file.">Competency Framework Package (JSON)</a></div>
		<div class="col-xs-3"><a href="{{ path('cfpackage_export2', {'id':lsDoc.id, '_format':'pdf'}) }}" target="_blank" role="button" tabindex="2" class="btn btn-primary btn-block" style="white-space:normal; height:3.8em"data-toggle="popover" data-trigger="hover" data-placement="bottom" data-html="true" data-content="Exports a styled PDF that can be printed, easily shared with others, and compared with other versions of the Framework.<hr>The PDF will open in a separate window; you can save or print the document from there.">Styled PDF</a></div>
		<div class="col-xs-3"><a href="{{ path('cfpackage_export2', {'id':lsDoc.id, '_format':'csv'}) }}" target="_blank" role="button" tabindex="3" class="btn btn-primary btn-block" style="white-space:normal; height:3.8em"data-toggle="popover" data-trigger="hover" data-placement="bottom" data-html="true" data-content="Exports a CSV file that you can open and edit in spreadsheet programs such as Microsoft Excel.<hr>The CSV file should immediately be downloaded.">Comma-Separated Values (CSV)</a></div>
		<div class="col-xs-3"><a href="{{ path('cfpackage_export2', {'id':lsDoc.id, '_format':'html'}) }}" target="_blank" role="button" tabindex="4" class="btn btn-primary btn-block" style="white-space:normal; height:3.8em"data-toggle="popover" data-trigger="hover" data-placement="bottom" data-html="true" data-content="Exports a zipped archive of the HTML and associated files (css, js, etc.) for viewing the Framework in a web browser. Use this if you want to include a view of the Framework in another website.<hr>The zip file should immediately be downloaded.">HTML Archive</a></div>
	</div></div>
	<hr>
	<p>You can also share the following link, which anyone can use to view the Competency Framework in their web browser (no login required):</p>

	<div class="form-horizontal">
	  <div class="form-group">
		<label for="inputEmail3" class="col-sm-6 control-label"><a href="{{ path('doc_tree_view', {'id':lsDoc.id}) }}" target="blank">{{ url('doc_tree_view', {'id':lsDoc.id}) }}</a></label>
		<div class="col-sm-6">
		  <input type="text" class="form-control" value="{{ url('doc_tree_view', {'id':lsDoc.id}) }}">
		</div>
	  </div>
	</div>
	
</div>
