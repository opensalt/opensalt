{% extends '::layout.html.twig' %}

{% block body %}
    {% if not app.request.xmlHttpRequest %}
        <h1>LsAssociation creation</h1>
    {% endif %}

    {{ form_start(form) }}
    
        <div class="container-fluid" style="margin-bottom:15px"><div class="row">
            <div class="col-sm-5 vcenter"><div class="lsAssociationItemDisplay" id="lsAssociationOriginDisplay"></div></div><!-- 
         --><div class="col-sm-2 vcenter" style="font-size:24px; text-align:center"><span class="glyphicon glyphicon-arrow-right" aria-hidden="true"></span></div><!-- 
         --><div class="col-sm-5 vcenter"><div class="lsAssociationItemDisplay" id="lsAssociationDestinationDisplay"></div></div>
        </div></div>
        
        {{ form_widget(form) }}
        {% if not app.request.xmlHttpRequest %}
            <input class="btn btn-primary" type="submit" value="Create" />
        {% endif %}
    {{ form_end(form) }}

    {% if not app.request.xmlHttpRequest %}
    <br/>
    <ul class="list-inline">
        <li>
            <a class="btn btn-default" href="{{ path('lsassociation_index') }}">Back to the list</a>
        </li>
    </ul>
    {% endif %}
{% endblock %}

{% block javascripts %}
{% endblock %}
