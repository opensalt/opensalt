{% extends 'layout.html.twig' %}

{% block title %}
    Issuer Registry List
{% endblock %}

{% block body %}
    <h1>Issuer Registry List</h1>

    {% if issuers is not empty %}
        <div class="row row-cols-3 g-4">
            {% for issuer in issuers %}
                <div class="col card-group">
                    <div class="card hover:secondary">
                        <div class="card-body link-opacity-25-hover">
                            <a class="stretched-link h3 card-title link-underline link-underline-opacity-0" href="{{ path('alt_issuer_registry_show', {'id': issuer.id}) }}">{{ issuer.name }}</a>
                            <p class="card-text">{{ issuer.notes }}</p>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endif %}
{% endblock %}
