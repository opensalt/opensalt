{% extends base_template %}

{% block body %}
    <h1>Group list</h1>
    <ul class="list-inline float-end">
        <li class="list-inline-item">
            <a class="btn btn-default" href="{{ path('admin_access_group_new') }}">Add a new access group</a>
        </li>
    </ul>
    <table class="table table-condensed table-hover table-bordered table-striped">
        <thead>
            <tr>
                <th class="col-md-1">Id</th>
                <th>Name</th>
                <th class="col-md-2">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for organization in organizations %}
            <tr>
                <td><a href="{{ path('admin_access_group_show', { 'id': organization.id }) }}">{{ organization.id }}</a></td>
                <td>{{ organization.name }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('admin_access_group_show', { 'id': organization.id }) }}">show</a>
                    <a class="btn btn-default" href="{{ path('admin_access_group_edit', { 'id': organization.id }) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
