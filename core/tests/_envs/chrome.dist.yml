# Chrome environment — connects to Selenium Grid hub
modules:
    config:
        WebDriver:
            browser: chrome
            host: selenium-hub
            port: 4444
            capabilities:
              chromeOptions:
                args: ['--headless', '--disable-gpu']
