#!/usr/bin/env sh
#<?php /*

if [ ! -f /.dockerenv ]; then
  exec $(dirname $0)/phpcli php ./bin/phpunit-real "$@"
fi

exec php $(dirname $0)/phpunit-real "$@"

*/

return require __DIR__.'/phpunit-real';

