ai:
    platform:
        generic:
            t2v:
                base_url: '%env(VECTOR_SEARCH_EMBEDDING_API_URL)%'
                api_key: '%env(VECTOR_SEARCH_EMBEDDING_API_KEY)%'
                model_catalog: 'App\VectorSearch\Platform\EmbeddingModelCatalog'

    agent:
        # Agent configuration
        # see https://symfony.com/doc/current/ai/bundles/ai-bundle.html

        # default:
        #    platform: 'ai.platform.openai'
        #    model: 'gpt-5-mini'
        #    prompt: |
        #        You are a pirate and you write funny.
        #    tools:
        #        - 'Symfony\AI\Agent\Bridge\Clock\Clock'

    store:
        # Store configuration

        # chromadb:
        #    default:
        #        client: 'client.service.id'
        #        collection: 'my_collection'
