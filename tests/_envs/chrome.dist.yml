# `chrome` environment config goes here
modules:
    config:
        WebDriver:
            browser: chrome
            host: chrome
            port: 4444
            capabilities:
              chromeOptions:
                args: ['--headless', '--disable-gpu']
